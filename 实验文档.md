# Mamba 选择性状态空间模型实验文档

## 1. 知识部分

### 1.1 状态空间模型的序列递推

状态空间模型（SSM）在每个时间步维护一个隐藏状态 `h_t`。给定当前输入
`x_t`，状态更新并产生输出：

```text
h_t = A_bar_t * h_(t-1) + B_bar_t * x_t
y_t = C_t * h_t + D * x_t
```

与 Transformer 通过 attention 在当前层直接聚合 token-token 关系不同，SSM 将
历史信息压缩在 `h_t` 中，沿时间维递推。因此其理论序列复杂度可以是线性的；但
实际速度还取决于 CUDA kernel、序列长度和硬件，不能仅由复杂度判断。

### 1.2 Mamba 的“选择性”

传统线性时不变 SSM 的参数固定，而 Mamba 让当前输入生成 `Δ_t`、`B_t`、`C_t`：

- `Δ_t` 控制状态更新的时间尺度；
- `B_t` 控制当前输入如何写入状态；
- `C_t` 控制从状态中读出什么；
- `D` 是逐通道 skip，保留无需经过状态的局部信息。

因此模型能随 token 内容决定信息应被写入、保留、遗忘或读出，而不是机械地以
固定规则递推。

### 1.3 本项目的 Mamba block 数据流

本实现的输入与输出均为 `[B, T, D]`。本次正式配置中 `D=128`、扩张倍数
`E=2`，所以内部通道数为 `d_inner=256`。

```text
[B,T,128]
  -> in_proj，分为 SSM 分支 x 和门控分支 z：各 [B,T,256]
  -> depthwise causal Conv1d + SiLU：x 为 [B,T,256]
  -> x_proj：低秩 Δ [B,T,8]，B/C 各 [B,T,16]
  -> dt_proj + softplus：Δ [B,T,256]
  -> selective scan：y [B,T,256]
  -> y * SiLU(z) -> out_proj：[B,T,128]
  -> Pre-RMSNorm 残差连接：[B,T,128]
```

本项目为教学实现，`selective scan` 使用 Python 时间循环，便于逐步对应递推式。
它不使用官方融合 CUDA kernel，故吞吐与显存不能用于代表官方 Mamba 性能。

## 2. 实验设计

### 2.1 数据协议

TinyStories token 流以 `uint16` 二进制文件保存。原验证流已在本地物理划分为：

| 数据 | token 数 | 用途 |
|---|---:|---|
| `data/train.bin` | 224,512,862 | 参数训练 |
| `data/val.bin` | 3,812,734 | 每 100 step 选择最佳 checkpoint |
| `data/test.bin` | 953,184 | 训练结束后独立评估 |

训练进程只加载 train/val；`evaluate.py` 只加载 test，因此 test 不参与模型选择。
每个自回归样本满足 `y[:, :-1] == x[:, 1:]`。

### 2.2 模型与训练配置

| 项目 | 实际值 |
|---|---|
| 模型 | 4 层纯 PyTorch 教学版 Mamba LM |
| 词表大小 | 50,257 |
| `block_size` | 128 |
| `d_model` / `d_state` / `d_conv` / `expand` | 128 / 16 / 4 / 2 |
| `dt_rank` | auto，即 `ceil(128/16)=8` |
| batch size / grad accumulation | 4 / 1 |
| 优化器 | AdamW，weight decay 0.1，betas `(0.9, 0.95)` |
| 学习率 | warmup 100 step 后 cosine 衰减，`3e-4 -> 3e-5` |
| 训练预算 | 1,000 optimizer step |
| validation | 每 100 step，10 个随机 batch |
| test | 固定 seed 42，100 个随机 batch，共 51,200 token |

训练在 AutoDL Linux RTX 3090 上完成。最终 test 使用同一 `best.pt`、同一
`test.bin` 在本地 RTX 3070 Laptop GPU 上执行；因此最终 loss/perplexity 可用，
但 test 的吞吐和显存只代表本地评估环境。

## 3. 实验结果

### 3.1 Checkpoint 与 validation

| 指标 | 实际结果 |
|---|---:|
| 训练完成 step | 1,000 |
| 最佳 checkpoint | `checkpoints/best.pt`，step 900 |
| 最佳 validation loss | 4.341955 |
| step 1000 validation loss | 4.346305 |
| step 1000 train loss | 4.37519 |

`best.pt` 来自 step 900，而不是机械选用最后一步；这是由 validation loss 的最小值
决定的。

### 3.2 最终 test

| 指标 | 实际结果 |
|---|---:|
| 使用 checkpoint | `best.pt`，step 900 |
| test loss | **4.3439** |
| test perplexity | **77.01** |
| 参与评估 token | 51,200 |
| test token 总量 | 953,184 |
| 本地评估吞吐 | 3,195 token/s |
| 本地评估峰值显存 | 325 MB |

test loss 与最佳 validation loss 接近（4.3439 vs. 4.3420），在这次固定随机 batch
估计下，没有显示出明显的 validation-to-test 落差。

### 3.3 训练性能记录

TensorBoard 在远端训练的最后一个记录区间显示：

| 指标 | 记录值 |
|---|---:|
| step 1000 最近区间吞吐 | 929 token/s |
| 训练记录中的最大峰值显存 | 518 MB |

这两个值是纯 PyTorch 逐步扫描实现、当前 batch 和短序列长度下的运行记录，不应与
官方 CUDA kernel、不同 GPU 或不同实现直接横向比较。

## 4. 结论与边界

1. 实验完整走通了物理隔离的 train/validation/test 流程：训练选择 `best.pt`，最终
   test 没有参与训练和 checkpoint 选择。
2. 在 1,000 step 的小型语言模型设置下，最佳 validation loss 为 4.3420，独立 test
   的固定采样估计为 4.3439（PPL 77.01）。
3. 结果证明该教学版 Mamba 的数据加载、因果序列建模、训练、checkpoint 恢复和独立
   test 评估链路有效；它不证明 Mamba 相对 Transformer 更快、更准确或可扩展到更长
   序列。
4. 当前 test 是 100 个固定随机 batch 的可复现实验估计，未遍历全部 953,184 个 test
   token；若需要报告更严格的全 test 指标，应将 `evaluate.py` 改为顺序遍历而非随机
   batch 采样。
5. 目前未生成文本样例，也未完成同预算的 Transformer 对照；这两项不能从本实验结果
   中推断。

## 5. 产物与复现

| 产物 | 位置 |
|---|---|
| 最佳模型 | `checkpoints/best.pt` |
| 最后一步 checkpoint | `checkpoints/latest.pt` |
| 训练与 test 曲线 | `tf-logs/mamba-formal-1000/` |
| 数据协议与训练代码 | `data_splits.py`、`train.py`、`evaluate.py` |

查看曲线：

```powershell
& 'E:\Miniforge\envs\mamba\python.exe' -m tensorboard.main --logdir tf-logs
```

TensorBoard 中应包含 `train/*`、`eval/*`、`performance/*` 与 `test/*` 四类标量。
